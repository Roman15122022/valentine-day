import {GifsProps} from "../types/types";


const Asked = ({classNames}: GifsProps) => {
  return (
    <div className={classNames}>
      <img src="asked.gif" alt=""/>
    </div>
  );
};

export default Asked;
